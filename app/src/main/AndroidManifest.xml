<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for dynamic application discovery on Android 11+ (API 30+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Required for dynamic network telemetry (Wi-Fi, VPN, Cellular state) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Required for Bluetooth adapter status detection on Android 10/11 -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />

    <!-- Official Termux Execution & Communication Permission -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_vajra_mark"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vajra">

        <!-- Receiver for Termux RunCommandService execution results -->
        <receiver
            android:name=".data.environment.TermuxResultReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.vajra.launcher.ACTION_TERMUX_RESULT" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="adjustPan|stateHidden">

            <!-- Android Home Launcher Integration -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Standard Application Launcher for Development & Normal Launching -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
